services:
  brain:
    build: ./gray-matter
    container_name: the-brain
    ports:
      - "8080:8080"
    volumes:
      - ./cortex:/cortex:ro
    environment:
      - VAULT_PATH=/cortex
    restart: unless-stopped
