services:
  brain:
    build: ./gray-matter
    container_name: the-brain
    ports:
      - "${HOST_PORT}:8080"
    volumes:
      - ./cortex:/cortex:ro
    environment:
      - VAULT_PATH=/cortex
    restart: unless-stopped
