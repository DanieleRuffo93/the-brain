services:
  brain:
    build: ./gray-matter
    container_name: the-brain
    ports:
      - "${HOST_PORT}:8080"
    volumes:
      - ./cortex:/cortex
    environment:
      - VAULT_PATH=/cortex
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
